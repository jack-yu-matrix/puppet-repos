---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "wso2/%{::product_name}/%{::pattern}/%{::product_profile}.yaml"
      - "wso2/%{::product_name}/%{::pattern}/common.yaml"
      - "wso2/common.yaml"
      - "nodes/%{trusted.certname}.yaml"
      - "common.yaml"