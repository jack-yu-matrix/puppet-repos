---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: "Yaml backend"
    data_hash: yaml_data
    paths:
      - "wso2/%{facts.product_name}/%{facts.pattern}/%{facts.product_profile}"
      - "wso2/%{facts.product_name}/%{facts.pattern}/common"
      - "wso2/common"
      - "nodes/%{trusted.certname}.yaml"
      - "common.yaml"
